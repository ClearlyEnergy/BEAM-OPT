from setuptools import setup, find_packages

version = '1.0'

setup(
    name='beam-opt',
    version=version,
    packages=find_packages(),
    description='',
    url='https://github.com/ClearlyEnergy/BEAM-OPT',
    )
